import { Account } from '../models/Account';
import { InterestRule } from '../models/InterestRule';

export const accounts: Map<string, Account> = new Map();

export const interestRules: InterestRule[] = [];
